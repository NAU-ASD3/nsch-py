"""Shared pytest fixtures and configuration for the nsch test suite.

Fixtures are added here as the package grows. Keep them small and explicit;
prefer constructing test data inline in test functions when a fixture would
only be reused once or twice.
"""

from __future__ import annotations
